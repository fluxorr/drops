export default function TodayPage() {
  return (
    <section className="empty-ledger" aria-labelledby="today-heading">
      <p className="ledger-date">Today</p>
      <h1 id="today-heading">Your ledger begins with a profile.</h1>
      <p>
        A short setup will give Drops enough context to choose a useful first lesson.
      </p>
    </section>
  );
}
